import { ToolDialogHostDirective } from './tool-dialog-host.directive';

describe('ToolDialogHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolDialogHostDirective();
    expect(directive).toBeTruthy();
  });
});
